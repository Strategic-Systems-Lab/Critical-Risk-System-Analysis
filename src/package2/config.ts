/**
 * Package 2 — Config
 * Flip DEMO_MODE to true to run Package 2 fully standalone with mock data,
 * independent of Package 1. Flip back to false for real integration.
 */
export const DEMO_MODE = false;
